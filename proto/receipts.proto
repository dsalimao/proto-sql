syntax = "proto3";

import "item.proto";
import "store.proto";

package proto;

message Receipts {
  int64 id = 1;

  int64 created_time = 2;

  int64 total_price = 3;

  Store store = 10;

  repeated Item items = 20;
}
